# Publicação na VPS — 28/08/2026

## Versão publicada

- Código: `d6949b53987ad4aa7ddcc1242a7a50f7666e5c67`.
- Backend: `whaticket-backend:safety-d6949b5`.
- Frontend: `whaticket-frontend:safety-d6949b5`.
- Aplicação: https://atendimento.bfontes.online.
- A revisão foi registrada em Git local e copiada para a VPS com histórico em bundle. Não foi configurado nem usado um remoto Git externo.
- Artefato: 891 arquivos conferidos por SHA-256. SHA-256 do pacote: `42cc6b5076c3eaf146cafd0f6f1f28700ca94931d17b4204e13c4d4b005cca19`.

## Continuidade e dados

A troca começou às 10:03:04 e a validação terminou às 10:03:37, horário de Brasília. Houve uma breve indisponibilidade do backend durante a substituição. Não foi executado logout; a sessão reconectou sem solicitar QR Code.

Após a publicação, a API autenticada e o banco confirmaram:

| Conferência | Resultado |
| --- | --- |
| Provedor | `whaileys`, mantido |
| WhatsApp | `CONNECTED`, sessão presente, QR vazio |
| Mensagens anteriores | 11.324 IDs presentes e conteúdo conferido individualmente por SHA-256 |
| Conversas anteriores | 1.102 tickets presentes |
| Contatos anteriores | 858 contatos presentes |
| Mídias anteriores | 98 arquivos conferidos por SHA-256 contra o backup |
| MariaDB e Redis | Mesmos containers e horários de início; não foram reiniciados |
| Backend e frontend | Imagens da revisão publicada, sem reinício inesperado ou OOM na conferência |
| API autenticada | Tickets, atendentes e cinco endpoints do painel Quark aprovados |
| Acesso público | Frontend e seus assets HTTP 200; API sem autenticação HTTP 401 |
| Navegador | Tela de login publicada carregou sem erro de console na inspeção |
| Quark | Sincronização inicial concluída com 292 registros, em passagem silenciosa |

As três migrations pendentes foram ensaiadas no clone e depois aplicadas à produção, sem seeds. A fila legada incompatível foi suprimida para revisão; não houve disparo em massa nem atribuição automática de consentimento.

## Backups

Na VPS, com acesso restrito a root:

- `/var/backups/whaticket/pre-safety-20260828`: cópia anterior usada no ensaio isolado.
- `/var/backups/whaticket/cutover-d6949b5-20260828T130303Z`: checkpoint após parar o backend e antes das migrations, contendo dump consistente, mídias, arquivos de autenticação, snapshot Redis, configurações e assinaturas das mensagens.

Integridade verificada com SHA-256, teste gzip e `redis-check-rdb`. As credenciais não foram colocadas no repositório. Esses backups estão na própria VPS; uma cópia externa criptografada continua sendo recomendada para recuperação de perda do servidor.

## Operação e reversão

`MESSAGING_MODE=production` foi configurado explicitamente para manter o atendimento. Provedor, segredos do Quark/JWT, volumes e configurações existentes foram preservados. Avisos proativos agora dependem de autorização; importar contatos não concede consentimento. A janela de atendimento vigente foi reconstruída a partir das mensagens recebidas.

Atualize a página após o deploy. Clientes próprios do endpoint HTTP de envio precisam de `Idempotency-Key`, conforme `messaging-safety-rollout.md`.

Em reversão, use as imagens anteriores (`whaticket-backend:quark-circuit-20260825-211500` e `whaticket-frontend:calendar-20260824-114200`) e a configuração salva, preservando o banco atual. Não restaure um dump sobre mensagens novas nem remova volumes, sessões ou estados da fila. Não execute versões antiga e nova simultaneamente.

Os testes não enviaram mensagem a pacientes nem alteraram consultas reais para validar entrega. O ensaio isolado e os testes unitários não garantem todas as condições de concorrência dos fornecedores. A API não oficial permanece sujeita a restrições; não há garantia de ausência de banimento.
